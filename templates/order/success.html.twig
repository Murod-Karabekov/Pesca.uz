{% extends 'base.html.twig' %}

{% block title %}Order Placed — Pesca{% endblock %}

{% block body %}
<section class="min-h-[70vh] flex items-center justify-center px-4 py-12">
    <div class="text-center max-w-md">
        <div class="w-20 h-20 mx-auto mb-6 bg-green-100 rounded-full flex items-center justify-center">
            <svg class="w-10 h-10 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
            </svg>
        </div>
        <h1 class="text-3xl font-display font-bold text-brand-dark mb-4">Order Placed!</h1>
        <p class="text-brand-medium leading-relaxed mb-8">
            Thank you for your order. Your request has been submitted successfully. We will contact you shortly to confirm the details.
        </p>
        <div class="flex flex-wrap gap-4 justify-center">
            <a href="{{ path('app_product_index') }}" class="btn-primary">Continue Shopping</a>
            <a href="{{ path('app_home') }}" class="btn-secondary">Back to Home</a>
        </div>
    </div>
</section>
{% endblock %}
